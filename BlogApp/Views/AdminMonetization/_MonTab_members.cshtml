@{
    var tab = (string)(ViewBag.ActiveTab ?? "overview");
}
@if (tab == "members")
{
    <div class="mon-empty">
        <p class="mon-empty-title">@T["mon.tab_members"]</p>
        <p class="mon-empty-hint">UI restore in progress</p>
    </div>
}
